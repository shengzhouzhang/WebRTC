
define(['dispatcher'],
  function (dispatcher) {
  'use strict';

  var actions = {

    // app
    APP_READY: 'APP_READY',

    // stores actions
    UPDATE_INSIDENT: 'UPDATE_INSIDENT',

  };

  return actions;
});
